[package]
name = "mergeable_errors"
version = "0.1.0"
authors = ["Pierre-Antoine Champin <pchampin@liris.cnrs.fr>"]
edition = "2018"

[dependencies]
error-chain = { version = "0.12.0", optional = true }

[features]
default = ["error-chain", "example_generated"]
example_generated = []